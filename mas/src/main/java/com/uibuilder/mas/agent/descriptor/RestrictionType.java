package com.uibuilder.mas.agent.descriptor;

/**
 * Types of restrictions that can be applied to UI components.
 */
public enum RestrictionType {
    MIN_WIDTH,
    MAX_WIDTH,
    MIN_HEIGHT,
    MAX_HEIGHT,
    REQUIRED_FIELD,
    NO_OVERLAP,
    ALIGNMENT_CONSTRAINT,
    COLOR_CONTRAST,
    ACCESSIBILITY_LEVEL
}
